query GetActivityDetails($id: ID!) {
  currentUser {
    activities(input: { filter: { id: $id } }) {
      pageInfo {
        totalCount
        hasNextPage
        hasPreviousPage
      }
      edges {
        cursor
        node {
          id
          name
          type
          description
          intensity
          type
          exrx
          muscleGroups
        }
      }
    }
  }
}
