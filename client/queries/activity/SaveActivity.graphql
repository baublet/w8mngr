mutation SaveActivity($input: ActivityInput!) {
  saveActivity(input: $input) {
    errors
    activity {
      id
      name
      type
      description
      intensity
      muscleGroups
      type
      exrx
    }
  }
}
