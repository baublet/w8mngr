query GetFoods($input: FoodQueryInput!) {
  currentUser {
    foods(input: $input) {
      pageInfo {
        totalCount
        hasNextPage
        hasPreviousPage
      }
      edges {
        cursor
        node {
          id
          name
          description
        }
      }
    }
  }
}
