[build]
  # Directory that contains the deploy-ready HTML files and assets generated by
  # the build. This is relative to the base directory if one has been set, or the
  # root directory if a base has not been set. This sample publishes the
  # directory located at the absolute path "root/project/build-output"
  publish = "client/"

  # Default build command.
  command = "yarn gql:generate && yarn build:api & yarn build:css && yarn build:client"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  # Directory with serverless functions, including background functions,
  # to deploy. This is relative to the base directory if one has been set, 
  # or the root directory if a base hasn’t been set.
  directory = "api/"
  included_files = ["api/*.js", "*.graphql"]
  external_node_modules = ["knex", "bcrypt"]